# Dependencies — never commit these, Vercel installs them itself
node_modules/

# Build output — generated by `npm run build`, not source
dist/

# Vercel deployment cache
.vercel/

# Local environment files
.env
.env.local
.env.*.local

# Editor / OS noise
.DS_Store
Thumbs.db
*.log
